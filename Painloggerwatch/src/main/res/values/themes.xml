<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme -->
    <style name="Theme.PainLogger" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand colors -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <!-- Use framework white for on-primary text/icons -->
        <item name="colorOnPrimary">@android:color/white</item>
        <item name="android:statusBarColor">?attr/colorPrimary</item>
        <item name="android:windowLightStatusBar">false</item>

        <!-- Secondary accent colors -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <!-- Use framework black for on-secondary text/icons -->
        <item name="colorOnSecondary">@android:color/black</item>

        <!-- Surface/background colors -->
        <item name="colorSurface">@color/white</item>
        <!-- Use framework black for on-surface text/icons -->
        <item name="colorOnSurface">@android:color/black</item>
        <item name="colorOutline">@color/colorOutline</item>
    </style>
    
    <!-- Custom time picker theme that ensures text is visible in both light and dark modes -->
    <style name="CustomTimePickerTheme" parent="@style/ThemeOverlay.MaterialComponents.TimePicker">
        <!-- Ensure text is visible in both light and dark modes -->
        <item name="colorOnSurface">@android:color/black</item>
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorOnPrimary">@android:color/white</item>
        <item name="colorOnSecondary">@android:color/black</item>
        <item name="android:textColorPrimary">@android:color/black</item>
        <item name="android:textColorSecondary">@android:color/black</item>
        <item name="android:textColor">@android:color/black</item>
        <item name="android:editTextColor">@android:color/black</item>
        <item name="android:textColorHint">@android:color/darker_gray</item>
    </style>
</resources>
